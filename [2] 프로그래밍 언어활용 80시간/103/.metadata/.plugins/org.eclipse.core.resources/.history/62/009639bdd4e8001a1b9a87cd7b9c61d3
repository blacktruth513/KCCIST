package Study;

// 2차원 배열의 경우 이중 for문을 사용 인덱스 표현 가능, 
// 2차원 배열 출력코드에 for문 사용 예제 p.64 [코드2-13]

class for2  {
	public static void main(String[] args) {
int arr2[][] = {{1,2,3}, {4,5,6}};
// 정수 값을 담는 arr2라는 이름의 2차원 배열을 선언하고 1행 각 배열 요소에 1,2,3을,
// 2행 각 배열 요소에 4,5,6을 저장 
for(int i=0; i<2; i++) {
//정수 값을 담는 i라는 변수에 0을 저장하고 i를 1씩 증가시키면서 i가 2보다 작을 동안 {}안을 반복
for(int j=0; j<3; j++) {
//정수 값을 담는 i라는 변수에 0을 저장하고 i를 1씩 증가시키면서 i가 3보다 작을 동안 {}안을 반복
	System.out.println("arr2["+i+"]["+j+"]: " + arr2[i][j]);
/* 문자열 "arr2[" 다음에 변수 i값, 다음 문자열 "][", 다음에 변수 j값, 다음에 문자열 "]:"
 * 다음에 arr[i][i] 변수 값을 시스템 콘솔에 출력 후 한 줄 띄움
 */
}
}
	}

}
